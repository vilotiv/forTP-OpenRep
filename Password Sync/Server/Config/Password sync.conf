###############################################################################
# Конфигурация центрального сервера Password Sync
###############################################################################

BASE_DIR="/opt/password_sync"

INBOX_DIR="${BASE_DIR}/inbox"
PROCESSING_DIR="${BASE_DIR}/processing"
ARCHIVE_DIR="${BASE_DIR}/archive"
FAILED_DIR="${BASE_DIR}/failed"

USER_MAP="${BASE_DIR}/config/user_map.conf"

PLAYBOOK="${BASE_DIR}/ansible/change-password.yml"
